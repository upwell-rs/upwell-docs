# Personal git identity for this repo. Copy to .env (gitignored) and fill in.
# Git reads these directly; no `git config` change needed.
GIT_AUTHOR_NAME="<NAME>"
GIT_AUTHOR_EMAIL="your-personal@example.com"
GIT_COMMITTER_NAME="<NAME>"
GIT_COMMITTER_EMAIL="your-personal@example.com"
